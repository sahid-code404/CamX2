package com.sahidcode404.camx.core.camera.model

enum class CameraTrust {
    UNKNOWN,
    ADVERTISED,
    VERIFIED,
    TEMPORARILY_UNAVAILABLE,
    STRUCTURALLY_REJECTED,
}

enum class PreviewTrust {
    UNKNOWN,
    ADVERTISED,
    VERIFIED,
    TEMPORARILY_UNAVAILABLE,
    STRUCTURALLY_REJECTED,
}

enum class RawTrust {
    UNKNOWN,
    ADVERTISED,
    VERIFIED,
    TEMPORARILY_UNAVAILABLE,
    STRUCTURALLY_REJECTED,
}
