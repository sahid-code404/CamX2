package android.media

/**
 * Source-only compatibility alias. Android exposes ImageFormat from android.graphics;
 * this keeps the camera owner source-compatible without introducing a runtime android.* class.
 */
typealias ImageFormat = android.graphics.ImageFormat
